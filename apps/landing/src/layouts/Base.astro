---
import '../styles/global.css';
import type { Copy, Locale } from '../i18n/types';
import { appStoreId, appStoreUrl } from '../i18n/utils';

interface Props {
  locale: Locale;
  meta: Copy['meta'];
}
const { locale, meta } = Astro.props;

const site = Astro.site ?? new URL('https://rinciku.com');
const canonical = new URL(Astro.url.pathname, site).href;
// Locale alternates for the *current* page: EN lives at the bare path, ID under /id/.
const basePath = Astro.url.pathname.replace(/^\/id(\/|$)/, '/');
const enUrl = new URL(basePath, site).href;
const idUrl = new URL(basePath === '/' ? '/id/' : `/id${basePath}`, site).href;
const ogImage = new URL('/og.png', site).href;

const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'Rinciku',
  applicationCategory: 'FinanceApplication',
  operatingSystem: 'Web, iOS',
  description: meta.description,
  url: site.href,
  installUrl: appStoreUrl,
  inLanguage: ['en', 'id'],
  offers: {
    '@type': 'Offer',
    price: '0',
    priceCurrency: 'IDR',
  },
  publisher: {
    '@type': 'Organization',
    name: 'Rinciku',
    url: site.href,
  },
};
---

<!doctype html>
<html lang={locale}>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1' />
    <link rel='icon' type='image/svg+xml' href='/favicon.svg' />
    <link rel='icon' type='image/png' sizes='32x32' href='/favicon-32.png' />
    <link rel='apple-touch-icon' sizes='180x180' href='/apple-touch-icon.png' />
    <link rel='manifest' href='/site.webmanifest' />
    <link rel='canonical' href={canonical} />
    <link rel='alternate' hreflang='en' href={enUrl} />
    <link rel='alternate' hreflang='id' href={idUrl} />
    <link rel='alternate' hreflang='x-default' href={enUrl} />

    <title>{meta.title}</title>
    <meta name='description' content={meta.description} />

    <meta property='og:type' content='website' />
    <meta property='og:site_name' content='Rinciku' />
    <meta property='og:locale' content={locale === 'id' ? 'id_ID' : 'en_US'} />
    <meta property='og:title' content={meta.title} />
    <meta property='og:description' content={meta.description} />
    <meta property='og:url' content={canonical} />
    <meta property='og:image' content={ogImage} />
    <meta property='og:image:width' content='1200' />
    <meta property='og:image:height' content='630' />
    <meta property='og:image:alt' content={meta.ogAlt} />

    <meta name='twitter:card' content='summary_large_image' />
    <meta name='twitter:title' content={meta.title} />
    <meta name='twitter:description' content={meta.description} />
    <meta name='twitter:image' content={ogImage} />

    <meta name='theme-color' content='#9ae600' />
    <meta name='generator' content={Astro.generator} />

    <!--
      Safari-on-iOS renders this as the native "Smart App Banner" at the top of
      the page (and flips to OPEN once the app is installed). Safari only — the
      CTA swap below is what reaches Chrome/Firefox on iOS.
    -->
    <meta name='apple-itunes-app' content={`app-id=${appStoreId}`} />

    <!--
      Stamp the platform on <html> before first paint so iOS visitors see the
      App Store CTA promoted without a flash of the web-first layout. Inline and
      synchronous on purpose: a bundled module would run too late. With JS off,
      no attribute is set and the web-first default stands.
    -->
    <script is:inline>
      (function () {
        const ua = navigator.userAgent;
        // iPadOS 13+ reports a Mac UA, so fall back to touch-point sniffing.
        const isIOS =
          /iPad|iPhone|iPod/.test(ua) ||
          (/Macintosh/.test(ua) && navigator.maxTouchPoints > 1);
        if (isIOS) document.documentElement.dataset.platform = 'ios';
      })();
    </script>

    <script
      type='application/ld+json'
      set:html={JSON.stringify(structuredData)}
      is:inline
    />
  </head>
  <body>
    <slot />
  </body>
</html>
